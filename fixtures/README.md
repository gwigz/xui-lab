# Fixtures

Fixtures describe deterministic viewer state, including inventory folders,
items, agent identity, avatar state, and cached names. Each fixture uses fixed
identifiers and works without a network connection.

The runtime adapter loads fixture data into the fork's real models. Fixtures do
not replace production models or copy their permission rules.
