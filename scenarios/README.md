# Scenarios

Scenarios will describe a subject, viewport, fixture, input steps, assertions,
and captured artifacts. Each scenario will run in a fresh process until the
viewer runtimes provide a proven reset boundary.

The first regression scenarios will cover an inventory item context menu and
the Inventory Explorer inspector placeholder at several widths.
